<UserControl x:Class="Budgets.GUI.WPF.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local1="clr-namespace:Budgets.GUI.WPF.Wallets"
             xmlns:local2="clr-namespace:Budgets.GUI.WPF.Authentication"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <DataTemplate DataType="{ x:Type local2:AuthViewModel}">
            <local2:AuthenticationView></local2:AuthenticationView>
        </DataTemplate>
        <DataTemplate DataType="{ x:Type local1:WalletMainViewModel}">
            <local1:WalletBaseView></local1:WalletBaseView>
        </DataTemplate>
    </UserControl.Resources>
    <ContentControl Content="{Binding CurrentViewModel}"></ContentControl>
</UserControl>

